---
title: Gotify
tags: ["Notifications"]
---

import Tags from "../../../components/Tags.astro"

[Homepage](https://gotify.net/) |
[Repository](https://github.com/gotify/server)

<Tags tags={frontmatter.tags} />

A simple server for sending and receiving messages in real-time per WebSocket. (Includes a sleek web-ui)

## Setting up

```yaml title="inventories/homelab/group_vars/homelab.yml"
gotify_enabled: true
```

The default login is `admin`/`admin`. Make sure to change the password after your first login!

By default, Gotify can be accessed at `http://[your_server_ip]:2346` or `https://gotify.[your_domain].com` if you have [DNS access](../guides/dns-access) configured.

## Configuration

See the default configuration options for Gotify at [`roles/gotify/defaults/main.yml`](https://github.com/Dylancyclone/ansible-homelab-orchestration/blob/main/roles/gotify/defaults/main.yml).
Add any overrides to your `inventories/[your_inventory]/group_vars/homelab.yml` file.

{/* 

## Breaking Changes

*/}