---
title: Lidarr
tags: ["*arr", "Music"]
---

import Tags from "../../../components/Tags.astro"
import RecentChanges from "../../../components/RecentChanges.astro"

[Homepage](https://lidarr.audio/) |
[Repository](https://github.com/Lidarr/Lidarr)

<Tags tags={frontmatter.tags} />

Lidarr is a music collection manager for Usenet and BitTorrent users. It can monitor multiple RSS feeds for new tracks from your favorite artists and will grab, sort and rename them. It can also be configured to automatically upgrade the quality of files already downloaded when a better quality format becomes available.

## Setting up

```yaml title="inventories/homelab/group_vars/homelab.yml"
lidarr_enabled: true
```

By default, Lidarr can be accessed at `http://[your_server_ip]:8686` or `https://lidarr.[your_domain].com` if you have [DNS access](../../guides/dns-access/) configured.

## Configuration

See the default configuration options for Lidarr at [`roles/lidarr/defaults/main.yml`](https://github.com/Dylancyclone/ansible-homelab-orchestration/blob/main/roles/lidarr/defaults/main.yml).
Add any overrides to your `inventories/[your_inventory]/group_vars/homelab.yml` file.

{/* 

## Breaking Changes

*/}

## Recent Changes

<RecentChanges application="lidarr" />
