---
exclude_paths:
  - .ansible/
  - .git/
  - .github/
  - roles/personal/

skip_list:
  - role-name[path] # Allow roles in `personal` folder
  # Some containers (like postgres) will automatically change file permissions on first run,
  # so we shouldn't fail when that happens, or change the permissions back
  - risky-file-permissions
